package com.cursor.automation.dal.model;

/**
 * Enum representing the status of a task in the data access layer.
 */
public enum TaskStatusEntity {
    TODO,
    IN_PROGRESS,
    BLOCKED,
    IN_REVIEW,
    DONE
} 