package com.cursor.automation.service.model;

/**
 * Enum representing the status of a task in the service layer.
 */
public enum TaskStatusService {
    TODO,
    IN_PROGRESS,
    BLOCKED,
    IN_REVIEW,
    DONE
} 